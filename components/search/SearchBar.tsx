"use client";

// Keyword input + category dropdown + search button
export function SearchBar() {
  return (
    <div className="flex gap-2">
      <p className="text-sm text-muted-foreground">SearchBar — coming soon</p>
    </div>
  );
}
