"use client";

// All browse filters (category, borough, price range, revenue, etc.)
export function FilterSidebar() {
  return (
    <aside className="space-y-4">
      <p className="text-sm text-muted-foreground">FilterSidebar — coming soon</p>
    </aside>
  );
}
